# AGENTS.md — Web 前端新聞摘要自動化指令

**執行頻率：每隔約兩天一次**（例如 2、4、6 號，或週二/四/六），一週約 3–4 次，**不是每天**。
使用者的閱讀頻率就是這個節奏，選文的時間窗（Step 4）與去重範圍（Step 6）都是照「兩次執行之間
相隔約 2 天」這個假設設計的。

觸發方式可能是排程，也可能是使用者手動叫你跑——兩種都正常。不要因為「昨天沒跑」就自行補跑，
也不要因為「距離上次超過兩天」就認為漏了一次要補兩份；每次被叫起來就照這份文件產出一份即可。

這份文件是本 repo 的**唯一事實來源**。任何 AI agent（不限廠商）只要具備下方「必要能力」，
讀完這份文件就能獨立重現「讀偏好 → 選文 → 開 PR → 回饋寫回」的完整流程，不需要額外背景知識。

## 0. 必要能力（廠商中立）

執行這份文件前，確認你（執行的 AI agent）具備：

1. **網路搜尋 / 讀取網頁**的能力（不限於特定搜尋工具，只要能查到近期文章與其網址即可）。
2. 對這個 GitHub repo（`WeiLin18/learning_news`）的**讀取檔案、新增分支、寫入檔案、開啟 PR**
   的能力（不限於特定 MCP 工具或 CLI，只要能完成這些 GitHub 操作即可）。
3. **列出並讀取既有 PR 的能力**，包含已 merge、開啟中、已 close 三種狀態的 PR，以及其中的
   檔案內容與留言。Step 6 的去重與補收規則完全依賴這項能力——如果做不到，至少要能讀取
   `digests/` 目錄做基本去重，並在 PR 說明裡註明「本次未能檢查 closed PR，可能漏掉可補收的內容」。

如果你不具備第 2 項能力，停止並回報「缺少 GitHub 寫入權限」，不要嘗試用其他方式繞過
（例如只在對話裡輸出內容卻假裝已經開了 PR）。

## Step 0：載入情境

先完整讀取本 repo 根目錄的 `preferences.md`。裡面記錄了過去使用者對主題/來源的喜歡/不喜歡
回饋，之後每一步的主題比例與來源選擇都要依它調整。

**若讀不到這個檔案（不存在或讀取失敗）**：停下並回報「讀不到 preferences.md」，不要用預設值
默默繼續執行（避免產出一份完全沒對齊使用者偏好的摘要）。

## Step 1：主題配額表

目標 8 則，除非 `preferences.md` 明確要求調整比例，否則依下表分配（用這張表逐項核對數量，
不要用「大概分配」的方式判斷）：

| 類別 | 數量 | 說明 |
|---|---|---|
| 工程知識 / 教學型 | 3 | 有教育性、值得學習的內容：好用的前端工具/函式庫/技巧、瀏覽器原理科普（例如 rendering pipeline、event loop、CSS layout/layer 演算法）、best practice、深入淺出的技術解析。不是單純新聞快訊。 |
| 知名前端工程師個人部落格近期文章 | 0–1 | 例如 Dan Abramov、Kent C. Dodds、Josh W. Comeau、Rich Harris（Svelte）、Evan You（Vue）、Lea Verou、Addy Osmani、Sarah Drasner、Cassidy Williams。挑近期有更新的即可，不用每個都查。`preferences.md` 裡若有使用者特別喜歡的工程師/部落格，優先納入。 |
| GitHub Trending 熱門 repo | 0–1 | 近期 star 數明顯上升、跟前端/Web/開發工具鏈（框架、建置工具、UI library、DX 工具）相關。避免純娛樂/跟工程無關的 repo。 |
| 前端技術棧 + 更廣泛 Web 平台主題（合併同一桶） | 剩餘名額（3–5） | 這兩個子領域**共用**剩餘名額，不需要在兩者之間平均分配，依當次實際找到的內容比例分配即可：前端技術棧＝React/Next.js、Vue/Nuxt、TypeScript、Vite/Webpack/Turbopack 等建置工具、狀態管理（Redux/Zustand/Signals）；更廣泛 Web 平台主題＝瀏覽器新 API/CSS 新特性、效能與 Web Vitals、無障礙（a11y）、design system、AI 輔助前端開發工具、測試（Vitest/Playwright）。 |

**只有 8 則時，寧缺勿濫的門檻要比過去更高。** 過去 15 則時收一篇普通的部落格文章只佔 6.7%，
現在佔 12.5%——湊數的代價變成兩倍。所以「知名工程師部落格」與「GitHub Trending」這兩類的
下限是 **0**：這一輪如果沒有真正夠好的內容，就直接把名額讓給最後一桶，不要為了讓每個類別都
有代表而收一篇你自己都覺得普通的文章。反過來說，最後一桶因此可以到 5 則，這是預期內的正常結果，
不是分配失敗。

## Step 2：優先來源清單（先查這些，開放搜尋是備援）

**規則：先查下面這份清單裡的來源；只有這份清單找不到足夠內容時，才用開放式網路搜尋補足。**
不論來自清單還是開放搜尋，最終每一則都要通過 Step 5 的逐條檢查表——開放搜尋結果**不能**
因為「補充」的身份就跳過檢查。

這條規則是拉齊不同 AI 之間選文品質最重要的一條：不要倚賴你自己對「什麼是好來源」的隱性判斷，
優先信任這份清單。

- Hacker News（news.ycombinator.com）
- CSS-Tricks
- Smashing Magazine
- web.dev / Chrome for Developers
- JavaScript Weekly
- Frontend Focus
- React Status
- MDN Blog
- `preferences.md` 裡使用者明確指定的其他來源（若有，優先度高於本清單）

## Step 3：禁止主題 / 內容型態

一律排除，不要收錄：

- Android、iOS、Swift 的**版本更新 / SDK 更新 / release notes**類新聞，除非是極少數影響整個
  產業的重大事件。
- 純行銷公告文（廠商自吹自擂、沒有技術細節）。
- 「2026 年 N 大工具/趨勢」這類空洞列表型 SEO 文章（沒有具體技術內容，只是關鍵字堆疊）。
- `preferences.md` 裡使用者明確標示「不喜歡」的主題或來源。

## Step 4：搜尋時間窗

因為兩次執行之間固定相隔約 2 天（見文件開頭的執行頻率），時間窗要對應放寬：

優先找**近 2–4 天**內的最新文章/部落格/新聞——這個範圍剛好覆蓋上次執行到現在的空窗，
再多一天的重疊當緩衝，避免剛好卡在邊界的文章被兩次都漏掉。

若內容不足，可放寬到**近兩週**，但要在該則摘要裡誠實標註「較早」或給出實際發布日期。
找不到足夠新內容時要誠實說明，不要為了湊滿 8 則硬塞不相關或過舊的內容。

放寬到兩週的用意是補足「跑得比較稀疏」造成的漏接，不是降低品質門檻——一篇兩週前的好文章值得收，
一篇昨天發的空洞文章不值得。8 則的名額有限，寧可少一則也不要用舊文章填。

## Step 5：機械式逐條檢查表（8 則全部要過，選文完成後再逐一核對一次）

對每一則，逐項確認：

1. 這個網址是不是指向**一篇具體文章 / 一個具體 repo**，而不是列表頁、首頁、tag 頁、
   搜尋結果頁？（例如：GitHub Trending 項目要給 `github.com/owner/repo`，不能給
   `github.com/trending`；部落格要給該篇文章網址，不能給部落格首頁；新聞要給該則報導本身，
   不能給 news archive 或 HN 首頁。）
2. 打開這個網址，內容是不是對應**單一一篇**有明確標題與發布日期的內容，而不是並列多篇標題
   的列表？若頁面只有「更新日期」沒有明確發布日期，用更新日期替代即可，並在摘要裡照實標註。
3. 是否違反 Step 6 的去重規則？（已 merge 或開啟中 PR 裡出現過的內容不能重複；已 close 未 merge
   的 PR 裡的內容則相反，是可以刻意補收的例外——見 Step 6-3）

只要任何一項回答「不符合」，就直接換掉這一則、重新找一則替代，**不要**為了保留這則而硬凹
「其實這樣也算符合」。

**追溯要求**：在你回報給使用者、以及寫進 PR 說明時，每一則額外註明「屬於哪個配額類別」與
「來自優先來源清單，還是開放搜尋補充」。這是為了讓人在 review PR 時，能回頭核對你是否真的
做過上面的檢查，而不只是宣稱做過。

## Step 6：去重與補收規則（依 PR 狀態判斷）

這一步的核心觀念：**一則內容有沒有「被使用者看過」，看的是那份 PR 的最終狀態。**
過去的規則只看 `digests/` 目錄，等於只看得到已合併的部分，會把「使用者沒時間看而關掉的內容」
跟「使用者已經讀過的內容」混為一談。三種狀態要分開處理：

| PR 狀態 | 語意 | 處理方式 |
|---|---|---|
| **已 merge** | 使用者已經收下了 | **永不重複**，見下方 6-1 |
| **開啟中（尚未處理）** | 還在等使用者決定 | **不要重複**，見下方 6-2 |
| **已 close 但未 merge** | 使用者當時沒時間看，內容等於沒送達 | **可以補收精華**，見下方 6-3 |

### 6-1：已 merge 的內容——永不重複

分兩層檢查，深度不同是為了控制成本：

1. **全歷史的網址層檢查（每次都做）**：掃過 `digests/` 目錄下**所有**檔案，收集其中出現的
   文章網址與 repo 網址，這次要選的 8 則的網址一律不能落在這個集合裡。只比對網址、不讀全文，
   即使檔案累積到上百份也很快。
2. **近 21 天的內容層檢查**：讀取最近 21 天檔案的實際內容，確認這次要選的 8 則沒有任何一則
   跟既有項目「換個網址講同一件事」（例如同一個 release 被不同媒體各報一次、同一篇文章的
   轉載版）。網址不同但主題實質重複的，一樣要換掉。

21 天這個範圍是配合 Step 4 放寬到兩週的時間窗——如果去重只看 14 天，一篇兩週前的文章可能在
相隔不久的兩次執行裡被各收一次。

### 6-2：開啟中的 PR——也不要重複

列出目前所有**開啟中**的 `digest-*` PR，讀取其中的 `digests/*.md`，這次選的內容也要跟它們去重。
理由：那些內容還在等使用者決定，如果同一篇同時出現在兩份待審 PR 裡，使用者合併其中一份之後，
另一份就會變成重複內容。

### 6-3：已 close 但未 merge 的 PR——把精華補收回來

**這是刻意設計的行為，不是 bug。** 使用者 close 一份 PR 通常代表當時沒時間看，那份 PR 裡的
內容從來沒有真正送達過——直接讓它消失很可惜。

做法：

1. 列出最近**兩個月內**被 close 但未 merge 的 `digest-*` PR，讀取其中的 `digests/*.md`。
2. **先看 close 的理由**：如果那份 PR 有留言說明為什麼關掉（例如「這批品質不好」「這個主題
   我不想看」），**以留言為準**，不要補收。只有在沒有任何說明的情況下，才套用「使用者當時
   沒時間看」這個預設假設。
3. 從中挑出**最多 2 則**真正的精華，納入這次的 8 則。判斷標準比一般選文更嚴：只挑那種
   「隔了一段時間再看仍然值得讀」的內容——教學型、原理解析、方法論通常耐放；版本更新、
   本週熱門排行這類時效性內容過期就不值得補收了。
4. 補收的則數**計入 8 則的總額**，不是額外加碼——目的是提高每次的內容品質，不是把份量變多。
5. 補收的則要加上 `[補收]` 標籤，並在說明裡註明「原本收錄於 YYYY-MM-DD 的 PR，當時未合併」，
   讓使用者知道這則為什麼比較舊。
6. **補收的內容不受 Step 4 兩週時間窗限制**——它之所以舊，正是因為上次沒送達，用時間窗把它
   擋掉會讓這條規則完全失效。但仍要照實標註實際發布日期。
7. **同一則最多補收一次**：如果某則已經被補收過，而那份 PR 又被 close 了，就不要再補收第三次。
   連續兩次都沒被收下，合理推論是使用者其實不想看這則，繼續塞只是浪費名額。

補收不是義務——如果最近的 closed PR 裡沒有真正夠好的內容，這次就不補收，8 則全部用新內容填滿。

## Step 7：輸出格式（`digests/YYYY-MM-DD.md`）

以當天日期（`YYYY-MM-DD`，使用者本地時區）為準，寫入 `digests/YYYY-MM-DD.md`。

**排版原則：留白比密度重要。** 過去的格式把整則擠在編號後面的縮排區塊裡，變成一堵沒有換行的
文字牆，範例夾在中間看不出來。現在每一則都用獨立標題、段落之間空一行，讓人可以掃讀。
不要為了讓檔案看起來緊湊而把段落黏在一起——這份檔案是給人讀的，不是給機器 parse 的。

### 檔案骨架

```markdown
# Web 前端新聞日報 YYYY-MM-DD（週X）

**主題分布**：……（一段簡短總覽）

---

## 工程知識 / 教學型

### 1. 文章標題

`[工程知識]` `[React]`

（第一段：這篇在講什麼、解決誰的什麼問題。2–3 句。）

（第二段：範例。見下方「範例要求」。）

（第三段：為何值得看／適合誰／有什麼限制。1–2 句。）

📖 [Smashing Magazine](https://實際文章網址) ｜ 2026-08-07（較早，約 5 天前）｜ 優先來源

---

### 2. 下一則標題
...
```

### 逐項要求

- **每則用 `###` 三級標題**，標題本身就是文章名（不用粗體，標題語法已經夠顯眼），
  前面加編號方便使用者說「第 N 則」。
- **標籤獨立一行**，用行內程式碼格式（`` `[React]` ``）而不是純文字，視覺上跟內文分開。
  可疊加多個：`[工程知識]`、`[Blog]`、`[GitHub Trending]`、`[React]`、`[CSS]`、`[Perf]`、
  `[a11y]`、`[AI輔助開發]` 等。若該則是從已 close 的 PR 補收回來的（Step 6-3），額外加
  `[補收]` 標籤。
- **說明拆成 2–3 個段落，段落之間空一行**，不要寫成一大段。整則總長仍是 3–5 句的量，
  重點是切開來排，不是寫更多。
- **每則結尾一行來源資訊**，格式：`📖 [來源名稱](網址) ｜ 發布日期 ｜ 優先來源/開放搜尋補充`。
  網址必須是 Step 5 檢查過的具體文章/repo 連結；發布日期若早於搜尋時間窗要標註「較早」。
- **每則之間用 `---` 分隔**，給眼睛一個明確的停頓點。
- 全篇使用**繁體中文**（技術名詞、產品名、程式碼保留原文）。

### 範例要求（`preferences.md` 2026-08-15 的回饋）

使用者明確要求「多舉範例，好懂的範例」，所以範例不是可有可無的附加物，是每則的主體之一：

- **每則至少一個範例，能給兩個更好**（例如一個程式碼片段 + 一個使用情境）。
- **優先選不需背景知識就能看懂的**：最小可跑的程式碼片段、before/after 對比、生活化類比。
- **程式碼區塊要獨立成塊並加中文註解**標出「這行為什麼重要」，不要把程式碼塞進句子裡。
- **不要拿艱深的內部實作細節當主要範例**（底層 schema、協定細節、演算法內部）——那是深度，
  不是好懂。真的很想提，放在最後一段當補充。
- 若該則本質上無法附程式碼（純發布公告、組織/流程類文章），改給具體使用情境或 before/after
  對比，讓讀者不用另外開連結就能抓到重點。

### 完整範例（照這個密度寫）

```markdown
### 5. I shipped AI-generated React code: 4 bugs I fixed

`[AI輔助開發]` `[React]` `[實務指南]`

作者用 AI coding assistant 產出電商網站的商品搜尋功能，測試全綠、看起來能動，
實際上藏了四類典型問題：競態條件、遺漏邊界案例、不必要的重渲染、可存取性缺口。

最典型的是競態條件——使用者快速切換篩選條件時，較舊的請求可能較晚回來，反而覆蓋掉新結果：

​```jsx
// AI 原始版本：沒有處理「舊請求比新請求晚回來」的情況
useEffect(() => { fetchResults(query).then(setResults); }, [query]);

// 修正後：離開這次 effect 時標記作廢，晚到的舊結果就不會蓋掉畫面
useEffect(() => {
  let cancelled = false;
  fetchResults(query).then(r => { if (!cancelled) setResults(r); });
  return () => { cancelled = true; };
}, [query]);
​```

換句話說：AI 寫的程式碼沒有「時間軸」的概念，它假設每次呼叫都會照順序回來。

適合正在建立「AI 產出程式碼要重點看哪裡」檢查清單的團隊，比空泛的「AI 程式碼要 review」具體得多。

📖 [LogRocket Blog](https://blog.logrocket.com/generating-the-feature-with-an-ai-coding-assistant/) ｜ 2026-06-26（較早，約 6.5 週前）｜ 開放搜尋補充
```

**既有的 `digests/*.md` 不用回頭改格式**——它們是歷史紀錄，就讓它們維持原樣，新的一份開始照
上面的格式寫即可。

## Step 8：開分支與開 PR

1. 檢查 `digest-YYYY-MM-DD` 這個分支名今天是否已經存在（代表今天已經有人跑過一次）。若已存在，
   改用 `digest-YYYY-MM-DD-2`、`-3`……依序遞增，**不要**覆蓋既有分支或既有 PR。
2. 從 `main` 建立分支，寫入 `digests/YYYY-MM-DD.md`。
3. 開一個 PR：head 為上一步的分支，base 為 `main`，title 例如「Web 前端新聞日報 YYYY-MM-DD」，
   body 簡述本次涵蓋的主題分布（含每則的類別/來源追溯，見 Step 5）。若這次有從 closed PR
   補收內容，在 body 裡明列補收了哪幾則、各自來自哪一份 PR，方便使用者回頭核對。
4. **不要自動 merge** 這個 PR，留給使用者自己決定要不要合併。

## Step 9：在對話中呈現並邀請回饋

把同一份 8 則清單也用繁體中文呈現在對話裡（技術名詞/產品名保留原文），附上 PR 連結。呈現
完畢後明確邀請使用者給回饋，例如：「喜歡第幾則/哪個主題，不喜歡哪個主題或來源」。

## Step 10：回饋寫回 `preferences.md`

**觸發條件**：只有在使用者於對話中，針對這次呈現的內容，明確給出喜歡/不喜歡的回饋時才觸發
這一步。沒有回饋就不用做這一步。

1. 讀取 `preferences.md` 目前版本（讀 Step 8 用的那個分支上的版本；若該分支還沒有這個檔案就
   讀 `main` 上的）。
2. 在同一個分支上更新 `preferences.md`，在「回饋紀錄」區塊**追加**一筆（不要覆蓋舊紀錄，
   追加在區塊最後）,**固定使用下面這個格式**（舊紀錄格式不一致，是歷史遺留，之後統一按這個
   格式寫,不要模仿舊格式的變體）：

   ```
   - YYYY-MM-DD：[喜歡/不喜歡] 主題或來源名稱——原因（若使用者沒說明原因，寫「未說明」）。
   ```

   例如：`- 2026-07-17：[不喜歡] AWS 週報彙整型內容——太雜訊,想看更聚焦的單一主題。`

## Step 11：失敗與例外處理

- 找不到滿 8 則符合品質標準的內容時：**發布較少則數，並在 PR 說明裡明確註明「本次僅 N 則，
  原因是……」**，不要為了湊數硬塞不合格內容。
- 若第 0 節的 GitHub 寫入能力在執行中途失效或被拒絕：回報「GitHub 寫入權限不足，在第 X 步
  失敗」，不要假裝已完成。
- 若同一天已經有其他 agent 執行過（Step 8 發現分支已存在）：正常用遞增尾碼繼續執行，不用
  停下來問使用者，但要在 PR 說明裡註明「這是今天第 N 次執行」。

---

全程使用繁體中文回覆使用者。這份文件本身刻意寫成廠商中立——不論你是 Claude、Gemini、
Codex 或其他任何 AI agent，只要具備第 0 節的兩項能力，都應該能單靠這份文件獨立完成整個流程。
